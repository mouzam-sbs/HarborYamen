

@model IEnumerable<BuyerSeller.Models.User>
@{
    ViewBag.Title = "Sellers";
    Layout = "~/Views/Shared/_LayoutPage1.cshtml";
}

<div class="box box-primary">
    <div class="box-header with-border">
        <h3 class="box-title">@GetValueByKey("AddBuyer")</h3>
    </div>
    <div class="box-body">

        <table class="table table-bordered table-striped table-condensed flip-content">
            <thead class="flip-content">
                <tr>

                    <td>@GetValueByKey("Image")</td>
                    <td>@GetValueByKey("Name")</td>
                    <td>@GetValueByKey("Email")</td>
                    <td>@GetValueByKey("Phone")</td>
                    @*<td>@GetValueByKey("Latitude")</td>
                     <td>@GetValueByKey("Longitude")</td>*@
                   
                    <td>@GetValueByKey("Address")</td>
                    <td>@GetValueByKey("Roles")</td>
                    <td>@GetValueByKey("Action")</td>


                  




                </tr>
            </thead>

            <tbody>
                @foreach (var item in Model)
            {
                    <tr>
                        <td>
                            @{
                                string base64Product = string.Empty;
                                string ProductImage = string.Empty;
                                if (item.ImageUpload != null)
                                {
                                    base64Product = Convert.ToBase64String(item.ImageUpload);
                                    ProductImage = string.Format("data:image/jpg;base64,{0}", base64Product);
                                }

                            }
                            <img src='@ProductImage' height="75" width="75" />

                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>


                        <td>
                            @Html.DisplayFor(modelItem => item.EmailID)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.MobileNo)
                        </td>



                        <td>
                            @Html.DisplayFor(modelItem => item.Address)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Role.Name)
                        </td>
                        <td>
                            <button class="btn btn-success" onclick="location.href='@Url.Action("ManageRole","Account",new { userid=@item.ID})'">@GetValueByKey("ManageRole")</button>
                        </td>
                    </tr>
 }

        </table>
    </div>
</div>
